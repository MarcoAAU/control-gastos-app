/**
 * Punto de entrada de la aplicación.
 *
 * Estructura definitiva (a partir de la Fase 6): providers + router.
 * Por ahora es un placeholder que confirma que el andamiaje funciona.
 */
export default function App() {
  return (
    <main style={{ padding: '2rem', fontFamily: 'system-ui, sans-serif' }}>
      <h1>Mis Gastos</h1>
      <p>Andamiaje v2 — React + Vite + TypeScript.</p>
    </main>
  );
}
